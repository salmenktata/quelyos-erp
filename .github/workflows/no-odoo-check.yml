name: No-Odoo Check

on:
  pull_request:
    branches: [main]
    paths:
      - 'vitrine-client/src/**'
      - 'dashboard-client/src/**'
      - 'vitrine-quelyos/src/**'

jobs:
  check-odoo-references:
    name: Vérifier références Odoo UI
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Scan violations P0
        id: scan
        run: |
          echo "🔍 Scanning for Odoo UI references..."

          # Exclusions
          EXCLUSIONS="legal/page.tsx|lib/odoo|odoo-rpc.ts|__tests__|node_modules"

          VIOLATIONS=""

          # Patterns P0 critiques
          for pattern in "dans Odoo" "via Odoo" "l'interface Odoo" "OdooImage" "VITE_ODOO_URL"; do
            MATCHES=$(grep -rl "$pattern" \
              vitrine-client/src \
              dashboard-client/src \
              --include="*.ts" --include="*.tsx" 2>/dev/null \
              | grep -vE "$EXCLUSIONS" || true)

            if [ -n "$MATCHES" ]; then
              VIOLATIONS="$VIOLATIONS\n❌ \"$pattern\":\n$MATCHES\n"
            fi
          done

          if [ -n "$VIOLATIONS" ]; then
            echo "## 🚨 Violations Odoo UI détectées" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo -e "$VIOLATIONS" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "### Actions requises" >> $GITHUB_STEP_SUMMARY
            echo "- Remplacer les références Odoo par des termes génériques" >> $GITHUB_STEP_SUMMARY
            echo "- Voir .claude/commands/no-odoo.md pour le mapping" >> $GITHUB_STEP_SUMMARY
            exit 1
          fi

          echo "## ✅ Aucune violation Odoo UI" >> $GITHUB_STEP_SUMMARY

      - name: Vérifier page legal préservée
        run: |
          if [ -f "vitrine-client/src/app/legal/page.tsx" ]; then
            if grep -q "Odoo Community Edition" vitrine-client/src/app/legal/page.tsx; then
              echo "✅ Page legal/ contient les mentions LGPL requises"
            else
              echo "⚠️ Page legal/ manque les mentions Odoo (conformité LGPL)"
              exit 1
            fi
          fi
