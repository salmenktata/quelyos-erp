<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Cron job : Exécution backups automatiques par tenant -->
        <record id="ir_cron_backup_schedules" model="ir.cron">
            <field name="name">Backups Automatiques (Tenants)</field>
            <field name="model_id" ref="model_quelyos_backup_schedule"/>
            <field name="state">code</field>
            <field name="code">model.cron_execute_scheduled_backups()</field>
            <field name="interval_number">15</field>
            <field name="interval_type">minutes</field>
            <field name="active" eval="True"/>
            <field name="priority">5</field>
        </record>
    </data>
</odoo>
