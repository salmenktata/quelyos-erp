apiVersion: v1
kind: Service
metadata:
  name: dashboard-client-service
  namespace: quelyos
  labels:
    app: dashboard-client
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: dashboard-client
