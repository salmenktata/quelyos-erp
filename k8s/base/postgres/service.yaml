apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: quelyos
spec:
  type: ClusterIP
  ports:
  - port: 5432
    targetPort: 5432
    name: postgres
  selector:
    app: postgres
  clusterIP: None  # Headless service for StatefulSet
