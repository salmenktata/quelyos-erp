apiVersion: v1
kind: Service
metadata:
  name: redis-service
  namespace: quelyos
spec:
  type: ClusterIP
  ports:
  - port: 6379
    targetPort: 6379
    name: redis
  selector:
    app: redis
  clusterIP: None  # Headless service for StatefulSet
